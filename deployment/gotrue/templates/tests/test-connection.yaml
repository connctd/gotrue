apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gotrue.fullname" . }}-test-connection"
  labels:
{{ include "gotrue.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "gotrue.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
